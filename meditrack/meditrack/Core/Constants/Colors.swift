import UIKit

// rule: (name)(number if needed)
extension Constants {
    enum Colors {
        static let grayAccent = UIColor(named: "GrayAccent")
        
        static let grayBackground = UIColor(named: "GrayBackground")
    }
}
